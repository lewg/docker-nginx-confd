[template]
keys        = [ "nginx/registries" ]
owner       = "nginx"
mode        = "0644"
src         = "registry.conf.tmpl"
dest        = "/etc/nginx/conf.d/registry.conf"
check_cmd   = "/usr/sbin/nginx -t -c /etc/nginx/nginx.conf"
reload_cmd  = "/usr/sbin/nginx -s reload"
